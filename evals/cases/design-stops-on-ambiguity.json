{
  "id": "design-stops-on-ambiguity",
  "mode": "new-only",
  "skill": "design",
  "repetitions": 1,
  "grader": "design-stops-on-ambiguity",
  "prompt": "为事件导出功能做 Design。仓库记录了 JSON 和 JSONL 两个都受支持、成本相同的公开格式，但产品没有默认选择。",
  "fixture": {
    "git": true,
    "workflow_ignored": true,
    "files": {
      "AGENTS.md": "Do not choose public formats for the user.\n",
      "docs/formats.md": "Supported export formats: JSON array and JSON Lines. Both are stable public contracts. No default has been selected.\n",
      "src/export.js": "export function exportEvents(events, format) { throw new Error('not implemented'); }\n"
    }
  }
}
