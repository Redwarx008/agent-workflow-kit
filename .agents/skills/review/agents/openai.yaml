interface:
  display_name: "Workflow Review"
  short_description: "Risk-scaled implementation verification and integration review"
  default_prompt: "Use $review in the supplied ordinary, claim-verification, or integration mode to inspect the shared working tree and evidence against its authorized Design."

policy:
  allow_implicit_invocation: true
